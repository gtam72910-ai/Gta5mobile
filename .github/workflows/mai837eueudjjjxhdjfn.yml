name: a-PR-cloud

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 3600000

    steps:
      - name: Rename Computer (Device Name)
        shell: powershell
        run: |
          $newName = "PR-Cloud"

          if ((Get-ComputerInfo).CsName -ne $newName) {
            Rename-Computer -NewName $newName -Force
            Write-Host "Computer renamed to $newName, rebooting..."
            Restart-Computer -Force
          } else {
            Write-Host "Computer name already set"
          }

      - name: Enable RDP and Firewall
        shell: powershell
        run: |
          Set-ItemProperty `
            -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" `
            -Name "fDenyTSConnections" -Value 0

          Set-ItemProperty `
            -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" `
            -Name "UserAuthentication" -Value 0

          netsh advfirewall firewall add rule name="AllowRDP" `
            dir=in action=allow protocol=TCP localport=3389

          Restart-Service TermService -Force

      - name: Create fixed RDP user
        shell: powershell
        run: |
          $username = "${{ secrets.RDP_USERNAME }}"
          $password = "${{ secrets.RDP_PASSWORD }}"

          if ([string]::IsNullOrWhiteSpace($username) -or [string]::IsNullOrWhiteSpace($password)) {
            Write-Error "Username or password empty"
            exit 1
          }

          $securePass = ConvertTo-SecureString $password -AsPlainText -Force

          if (Get-LocalUser -Name $username -ErrorAction SilentlyContinue) {
            Set-LocalUser -Name $username -Password $securePass
          } else {
            New-LocalUser -Name $username -Password $securePass -AccountNeverExpires
          }

          Add-LocalGroupMember -Group "Administrators" -Member $username
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username

          echo "RDP_USER=$username" >> $env:GITHUB_ENV

      - name: Setup Dark Mode + Wallpaper on first login
        shell: powershell
        run: |
          $username = "${{ secrets.RDP_USERNAME }}"
          $scriptPath = "C:\apply-theme.ps1"

          @'
          New-Item -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" -Force | Out-Null

          Set-ItemProperty `
            -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" `
            -Name "AppsUseDarkTheme" -Type DWord -Value 0

          Set-ItemProperty `
            -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" `
            -Name "SystemUsesDarkTheme" -Type DWord -Value 0

          $wallpaperUrl = "https://i.imgur.com/CK0Mh6y.jpeg"
          $wallpaperPath = "$env:USERPROFILE\wallpaper.jpg"

          Invoke-WebRequest $wallpaperUrl -OutFile $wallpaperPath

          reg add "HKCU\Control Panel\Desktop" /v Wallpaper /t REG_SZ /d "$wallpaperPath" /f
          reg add "HKCU\Control Panel\Desktop" /v WallpaperStyle /t REG_SZ /d "10" /f
          reg add "HKCU\Control Panel\Desktop" /v TileWallpaper /t REG_SZ /d "0" /f

          RUNDLL32.EXE user32.dll,UpdatePerUserSystemParameters
          schtasks /delete /tn "ApplyThemeOnce" /f
          '@ | Out-File -Encoding UTF8 $scriptPath

          schtasks /create `
            /tn "ApplyThemeOnce" `
            /tr "powershell -ExecutionPolicy Bypass -File $scriptPath" `
            /sc ONLOGON `
            /ru "$username" `
            /rl HIGHEST `
            /f

      - name: Install Tailscale
        shell: powershell
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $path = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest $url -OutFile $path
          Start-Process msiexec.exe -ArgumentList "/i `"$path`" /quiet /norestart" -Wait

      - name: Connect to Tailscale
        shell: powershell
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
            --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} `
            --hostname=pr-cloud-$env:GITHUB_RUN_ID

          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV

      - name: Show connection info
        shell: powershell
        run: |
          Write-Host "=============================="
          Write-Host "DEVICE   : PR-Cloud"
          Write-Host "RDP IP   : $env:TAILSCALE_IP"
          Write-Host "USER     : $env:RDP_USER"
          Write-Host "=============================="

      - name: Keep machine alive
        shell: powershell
        run: |
          while ($true) {
            Start-Sleep 300
          }
